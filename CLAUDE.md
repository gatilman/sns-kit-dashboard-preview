# CLAUDE.md — sns-kit-dashboard-preview 지침서 (규칙 + 현재 상태, 한 파일)

> 이 프로젝트 루트에 그대로 두는 파일입니다. 클로드 코드가 세션 시작 시 자동으로 읽습니다.
> 위쪽 "규칙"은 이 레포 고유 규칙. 아래쪽 "현재 상태"는 매 세션 갱신합니다.
> ⚠️ 이 레포는 사용자 홈 `~/.claude/CLAUDE.md`(전역 지침)도 항상 같이 로드된다 — 옵시디언 확인·기록,
> 한국시간(KST) 시·분·초 확인, git push 자율 실행 등은 거기서 이미 정해져 있으니 여기서 반복하지 않는다.
> ⚠️ **이 레포는 검토용 설계 시안이지 실제 상용 레포가 아니다** — 실제 SNS 자동화 로직/계정 정보를 여기 넣지 않는다.

---

## 1. 이 프로젝트

- 이름: sns-kit-dashboard-preview (GitHub: gatilman/sns-kit-dashboard-preview, **Public** 레포)
- 실제 정체: 대시보드라기보다 **"설치 마법사"에 가깝다.** 레포 전체가 파일 하나 — `index.html` (33KB, `<title>SNS 자동화 키트 — 설치 마법사</title>`) — 뿐이고, README·package.json·빌드 설정·다른 소스 파일이 일절 없다(2026-09-04 세션에서 `ls`로 직접 확인).
  - 목적: **크몽에서 "소상공인 SNS 자동화 키트"를 구매한 사람이** GitHub 계정만 가지고 스스로 설치를 끝낼 수 있게 하는 셀프서비스 온보딩 페이지. 화면 안 문구에 "구매자 전용 자료", "크몽 메시지로 문의" 등이 그대로 적혀 있어(직접 확인) 실제 판매 흐름용으로 만들어진 화면 시안임이 분명하다.
  - 6단계 구성: ①GitHub 개인 액세스 토큰(PAT) 발급 안내 → ②`gatilman/sosang-sns-kit`을 템플릿으로 구매자 본인 계정에 레포 생성(`POST /repos/gatilman/sosang-sns-kit/generate`) → ③ANTHROPIC/GEMINI/OPENAI, GOOGLE_*, INSTAGRAM/THREADS, GOOGLE_OAUTH(블로그/유튜브용), PEXELS, CLOUDINARY, LICENSE_*, TELEGRAM, TYPECAST 등 약 20개 GitHub Actions Secrets를 브라우저에서 libsodium(`crypto_box_seal`)으로 암호화해 업로드 → ④워크플로우(`daily-pipeline.yml` 등 5개) 수동 실행(workflow_dispatch) → ⑤실행 이력 조회(`/actions/runs`) → ⑥구글시트 "설정" 탭 값 읽기 전용 조회(구글 서비스계정 JWT를 브라우저에서 직접 서명).
  - **⚠️ 중요 — 이건 가짜 목업이 아니라 실제로 동작하는 페이지다.** 방문자가 진짜 GitHub PAT를 입력하면 진짜로 레포가 생성되고, 진짜 Actions Secrets가 등록되며, 진짜 워크플로우가 실행되고(Actions 사용량 소모), 진짜 구글시트에 접근한다. "검토용/미리보기"라는 레포 설명과 달리 **코드 자체는 프로덕션 로직 그대로** — 테스트 삼아 실제 PAT를 넣으면 진짜 부작용이 생긴다는 걸 항상 인지할 것.
  - 서버가 없다 — GitHub REST API, Google OAuth2/Sheets API와 브라우저에서 직접 통신할 뿐(자체 백엔드도, 판매자 서버 경유도 없음). 페이지 자체 문구에도 "판매자 서버 안 거침"이라고 명시.
- 운영 방식: **GitHub Pages가 이미 켜져 있고 `main` 브랜치 루트에서 서빙 중** — `main`에 push하면 그대로 https://gatilman.github.io/sns-kit-dashboard-preview/ 에 반영된다(2026-09-04 `gh api .../pages` 로 직접 확인, `status: built`). 별도 빌드/배포 명령이 없다 — 로컬 미리보기는 `index.html`을 브라우저로 그냥 열면 되고, 실제 배포본 확인은 위 Pages URL을 열면 된다.
- 짝을 이루는 다른 레포:
  - **`gatilman/sosang-sns-kit`** (Private, 존재 확인됨) — 이 설치 마법사가 실제로 생성/설정해주는 대상 템플릿 레포. 코드 레벨로 직접 import되는 관계는 아니고, 이 페이지가 그 레포 이름을 URL에 하드코딩해서 GitHub API로 호출하는 관계다.
  - giltda-sns-kit / edubutler-sns-kit — [미확인 — 이번 세션에서 코드로 직접 확인 안 함] 사용자 메모리(`feedback_sns-kit-core-logic-stays-serverside`)에 따르면 sosang-sns-kit은 본사(giltda) 원본에서 핵심 로직을 뺀 판매용 동기화 복사본이라고 되어 있으나, 이 세션에서는 sosang-sns-kit 저장소 내부를 열어보지 않았으므로 그 관계 자체는 재확인 필요.
  - 참고로 이 코드가 다루는 자동화 대상(인스타그램/쓰레드/블로그스팟/유튜브/구글시트 콘텐츠 파이프라인)은 길트다/에듀버틀러류 SNS킷과 같은 계열 기능이다.

## 2. 절대 원칙 (항상 적용)

1. **이 레포는 시안/검토용이다** — 실제 고객 데이터, 실제 API 키/계정 정보, 실제 GitHub PAT를 여기 커밋하지 않는다. 실제 데이터가 필요하면 가짜 예시 데이터를 쓴다.
2. **이 페이지를 직접 열어 테스트할 때는 항상 "이건 실제 GitHub/Google API를 진짜로 호출한다"는 걸 전제한다.** 값을 입력하고 버튼을 누르면 진짜 레포 생성/Secrets 등록/워크플로우 실행이 일어난다 — 스크린샷/디자인 검토 목적이면 값을 입력하지 않고 화면 레이아웃만 확인한다.
3. **하드코딩 관련 — 이 레포는 일반적인 "업종 하드코딩 금지" 원칙과 성격이 다르다.** giltda/edubutler 같은 코어 SNS킷 엔진은 여러 고객에게 파는 범용 제품이라 업종별 값을 하드코딩하면 안 되지만, 이 페이지는 애초에 "sosang-sns-kit 하나를 설치해주는 전용 온보딩 화면"이라 `gatilman/sosang-sns-kit` 같은 레포명 하드코딩은 이 도구의 존재 목적 자체다. 다만 **구매자 개인 정보(레포 이름, 토큰, 시트 ID 등)를 이 파일에 예시 이상으로 박아넣는 것**은 여전히 금지.
4. **"완료했습니다"는 아래 3장 체크리스트를 실제로 통과한 뒤에만 쓴다.** 통과 못 했으면 "OO까지 됐고 XX 미통과"라고 정확히 보고한다.
5. 3장이 미통과인 상태에서 4장(사람 취향)을 먼저 묻지 않는다.

## 3. 완료 전 체크리스트 (기계적으로 참/거짓 판별 가능한 것만)

- [ ] 빌드/타입체크: **해당 없음** — 정적 HTML 파일 하나뿐이고 package.json/빌드 도구가 없다. 대신 `index.html`을 브라우저로 열어 콘솔 에러 없이 렌더링되는지 눈으로 확인한다.
- [ ] **실제 API 키/계정 정보/고객 데이터(실제 PAT, 실제 시트 ID, 실제 이메일 등)가 섞여 들어가지 않았는지 diff로 확인함**
- [ ] 커밋 전 diff를 직접 읽고 의도치 않은 변경이 없는지 확인함
- [ ] `index.html`을 실제로 브라우저에서 열어 6단계 화면이 다 나타나고 레이아웃이 깨지지 않는지 확인함(값 입력/버튼 클릭은 절대원칙 2번 때문에 하지 않아도 됨 — 렌더링 확인이 목적)
- [ ] GitHub Pages 배포본(https://gatilman.github.io/sns-kit-dashboard-preview/)이 로컬에서 커밋한 최신 내용과 같은지 push 후 재확인함(별도 배포 절차 없이 push만으로 반영되는 구조이므로)

## 4. 사람이 판단할 것 (체크리스트에 넣지 않음)

디자인, 레이아웃, 문구 톤, 6단계 순서/구성 자체를 바꿀지 등. → 2~3개 선택지를 만들어 제시하고 고르게 한다.

## 5. 능동적 제안 원칙 — 시키는 대로만 하지 않는다

- 시킨 방식대로 구현하는 게 더 느리거나, 나중에 유지보수가 힘들거나, 더 나은 대안(성능/구조/재사용성)이 있다고 판단되면, **묻지 않아도 먼저 말한다.** "일단 시킨 대로 하고 나중에 언급"하지 않는다 — 그 시점엔 이미 늦다.
- **판단이 아니라 근거로 제안하되, 근거의 비용은 가장 싼 것부터 쓴다.** "이게 더 나을 것 같습니다"는 추측일 뿐이지만, 그렇다고 매번 두 벌을 다 만들어서 실측할 필요는 없다. 아래 순서대로, 앞 단계에서 판단이 서면 다음 단계로 넘어가지 않는다.
  1. 먼저 이미 아는 것으로 판단한다. 라이브러리 공식 문서·벤치마크, 이전에 이 프로젝트에서 이미 검증된 사례, 일반적으로 잘 알려진 사실만으로 결론이 나면 그걸로 끝 — 아무것도 새로 만들지 않는다.
  2. 그걸로 부족하면 전체가 아니라 병목이 되는 핵심 로직 한 조각만, 최소 샘플로 스파이크한다. 별도 worktree까지 가지 않고 스크래치 폴더나 임시 스크립트로 "빠르게 지우고 다시 만들 수 있는" 방식으로 확인한다. 전체 데이터가 아니라 대표성 있는 소량 샘플로 재본다.
  3. 위 두 단계로도 판단이 안 서는, 구조 전체를 바꾸는 수준의 결정일 때만 격리된 worktree(`git worktree` / `claude --worktree`)에 두 방식을 각각 구현해서 실제로 돌려보고 수치(속도, 에러율, 결과물 품질 지표 등)로 비교한다.
  - 어느 단계에서 판단했든 "이런 근거로 이쪽이 낫습니다"라고 근거의 수준(추정인지 실측인지)을 밝히고 제시한다.
- 🔴 **비용·토큰이 많이 드는 확인 작업(2·3단계, 특히 3단계)은 시작 전에 먼저 말하고 승인받는다** — 판단 근거를 만드는 과정 자체가 공짜가 아니다.
- 실측 비교든 아니든, 마음대로 다른 방식으로 바꿔서 본 작업에 반영하지는 않는다. 비교 결과를 보여주고 "어느 쪽으로 반영할까요?"라고 물은 뒤에만 본 작업에 적용한다.
- 작업 도중 "이 부분은 지금 방식대로 가면 나중에 반드시 문제가 생긴다"는 게 보이면, 하던 작업을 끝까지 다 하고 나서 언급하지 않고 **그 시점에 바로** 멈추고 알린다.
- 이 원칙은 "시키지 않은 일을 마음대로 벌인다"는 뜻이 아니다. 실행 여부의 최종 결정은 항상 사람이 한다 — 클로드는 판단 근거와 실측 비교까지 준비해서 올리는 역할까지만 한다.
## 6. 현재 상태 (Last updated: 2026-09-04 19:19:01 KST) — 매 세션 갱신

- 진행 중인 작업: 없음(이번 세션은 CLAUDE.md 신설만 진행)
- 방금 완료한 것: 이 레포에 CLAUDE.md 신설 — 클론 후 `index.html`(레포의 유일한 파일) 전체를 직접 읽고, GitHub Pages 배포 상태·`sosang-sns-kit` 존재 여부까지 `gh` CLI로 실측 확인한 뒤 작성.
- 3장 체크리스트 통과 여부:
  - 빌드/타입체크: 해당 없음(정적 파일이라 대상 자체가 없음) — 통과 처리
  - 실제 키/데이터 미포함 diff 확인: 통과 (CLAUDE.md 신규 작성 파일이며 실제 값 없음, `index.html`도 이번 세션에서 읽기만 하고 수정 안 함)
  - 커밋 전 diff 직접 읽음: 커밋 시점에 수행 예정
  - `index.html` 브라우저 렌더링 확인: **미수행** — 이번 세션은 문서 작업 범위였고 브라우저 도구를 쓰지 않았음. 다음 세션에서 화면을 실제로 만질 일이 있으면 그때 수행할 것.
  - Pages 배포본 재확인: CLAUDE.md만 추가하는 것이라 Pages 화면 자체에는 영향 없음(Pages는 `index.html` 기준으로 서빙됨) — 별도 확인 불필요로 판단.
- 사람 승인/선택 대기 중: 없음
- 다음에 할 일: 특별히 없음. 다음 세션에서 화면/문구를 실제로 고칠 일이 생기면 위 체크리스트(특히 브라우저 렌더링 확인)를 그때 수행할 것.
- 막힌 것 / 물어봐야 하는 것: sosang-sns-kit과 giltda-sns-kit의 정확한 코드 관계는 이번 세션에서 직접 코드로 확인하지 못했다([미확인] 표시해둠) — 필요하면 다음 세션에서 sosang-sns-kit 레포를 열어 확인할 것.
- 이번 세션에서 반복될 뻔한 실수 → 규칙으로 반영: 해당 없음(CLAUDE.md 신설 세션)

**세션을 끝낼 때 이 6장을 반드시 갱신하고 마친다.**
